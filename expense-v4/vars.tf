# here we re declaring the common variables tht we are using in expense project
variable "ami" {}
variable "sg_id" {}
variable "zone_id" {}
variable "env" {}
variable "components" {}

# Now we re providing the input of these variables in the input file(dev.tfvars).